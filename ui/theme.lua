-- /ui/theme.lua
-- Optional theme (loaded if present). Edit colors here to reskin the HUD.

theme = {}
theme.colors = {
  bg      = colors.black,
  text    = colors.white,
  muted   = colors.lightGray,
  card    = colors.black,
  good    = colors.green,
  warn    = colors.orange,
  bad     = colors.red,
  accent  = colors.blue,
  accent2 = colors.lime,
  btnOn   = colors.green,
  btnOff  = colors.gray,
}
